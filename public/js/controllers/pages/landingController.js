'use strict';

/* Controllers */
// Hubs controller (admin mode)
app.controller('LandingController', [
  '$scope',
  '$rootScope',
  '$modal',
  '$state',
  'growl',
  'API',
  'NetminoUtils',
  function($scope,
           $rootScope,
           $modal,
           $state,
           growl,
           API,
           NetminoUtils) {

  }]);
